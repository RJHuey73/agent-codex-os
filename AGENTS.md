<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. `package.json` pins `next@16.2.4` (react/react-dom `19.2.4`) — well past your training cutoff. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code (run `npm install` first if `node_modules/` isn't present). Heed deprecation notices.
<!-- END:nextjs-agent-rules -->
